import React from "react";
import ReactDOM from "react-dom/client";
import App from "@vyapar/ui";
import "@vyapar/ui/index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
