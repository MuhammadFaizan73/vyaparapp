# Local dev (PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/vyapar?schema=public"

# Local dev (SQLite — only if no PostgreSQL installed)
# DATABASE_URL="file:./dev.db"

PORT=3000
JWT_SECRET="change-me-in-production-use-a-long-random-string"

# SMS OTP (mobile number verification) — from your MSG91 dashboard.
# MSG91_AUTH_KEY="..."
# MSG91_TEMPLATE_ID="..."
